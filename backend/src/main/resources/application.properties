spring.application.name=page-pulse

server.port=${PORT:8080}

# Allowed origin for the frontend (set via environment variable in production)
app.cors.allowed-origin=${FRONTEND_ORIGIN:http://localhost:5173}
